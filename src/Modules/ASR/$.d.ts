/**
 * Count the number of deletions, insertions, or substitutions required
 * to transform str1 into str2. The number shows a measure of the
 * similarity between two strings. It is also known as edit distance.
 *
 * @param str1 First string.
 * @param str2 Second string.
 */
declare function levenshtein_distance(str1: string, str2: string): number;
