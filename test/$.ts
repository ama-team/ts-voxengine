const recoded: string = base64_encode(base64_decode('YmFzZTYzLjU='));
const uppercase: string = bytes2hex(hex2bytes('ffaa'), true);
const boring: string = bytes2str(str2bytes(uuidgen()));
clearInterval(setInterval(() => {}, 1000));
clearTimeout(setTimeout(() => {}, 1000));

